app_name: "citizen-x"
deployment_metadata:
  version: "1.0.0-Σ∞"
  sync_id: "98628-03482-53421"
  lexicon_status: "HARD_CODED_IN_MAIN"

services:
  main_brain:
    image: "citizen_x.png"
    container_name: "citizen-x-instance"
    restart: "unless-stopped"
    environment:
      - DATA_SOURCE=INTERNAL_LIST
      - PROTOCOL=DEADWOOD_MASK
    # No volumes needed - data is inside the image
    networks:
      - encrypted_mesh

networks:
  encrypted_mesh:
    driver: bridge
    
